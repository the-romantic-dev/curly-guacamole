extends: baseline_mixed_original.yaml
paths:
  run_name: pvt_v2_b2_protocol_baseline
dataset:
  protocol_path: runs/validation_protocol_20260908/protocol
  train_originals: false
train:
  resume: true
eval:
  resolution: original
