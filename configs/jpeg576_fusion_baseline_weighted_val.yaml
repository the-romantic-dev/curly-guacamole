extends: jpeg576_pretrained.yaml
paths:
  run_name: jpeg576_fusion_baseline_weighted_val
model:
  jpeg_variant: baseline
  fusion_variant: baseline
  norm: group
  aux_weight: 0.0
eval:
  small_mask_weight: 1.6
