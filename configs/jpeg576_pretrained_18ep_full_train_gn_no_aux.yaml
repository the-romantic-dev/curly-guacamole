extends: jpeg576_pretrained.yaml
paths:
  run_name: jpeg576_pretrained_18ep_full_train
train:
  # Sixteen sampled epochs, then two complete shuffled passes without replacement.
  epochs: 18
  epoch_size: 24000
  full_train_epochs: 3
augmentation:
  final_full_frame_epochs: 3
model:
  norm: group
  aux_weight: 0.0