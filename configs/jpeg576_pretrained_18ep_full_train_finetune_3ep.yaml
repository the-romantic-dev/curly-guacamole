extends: jpeg576_pretrained_18ep_full_train.yaml
paths:
  run_name: jpeg576_pretrained_18ep_full_train_finetune_3ep
train:
  # Relative to runs_path. Start from epoch 18 model weights with fresh optimizer/EMA.
  finetune_from: jpeg576_pretrained_18ep_full_train/ckpt/last.pt
  resume: true
  epochs: 3
  full_train_epochs: 3
  encoder_lr: 0.00001
  fmap_lr: 0.00003
  lr: 0.00003
  warmup_frac: 0.0
augmentation:
  full_frame_probability: 1.0
  final_full_frame_epochs: 3
